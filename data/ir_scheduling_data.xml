<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="scheduling_job" model="ir.cron">
        <field name="name">Update Scheduling data</field>
        <field name="model_id" ref="model_tx_hr_scheduling"/>
        <field name="state">code</field>
        <field name="code">model.update_data()</field>
        <field name='interval_number'>1</field>
        <field name='interval_type'>months</field>
<!--        <field name="nextcall"-->
<!--               eval="(DateTime.now().year,DateTime.now().month.replace(hour=0, minute=0, second=0) + timedelta(days=2)).-->
<!--                  strftime('%Y-%m-%d %H:%M:%S')"/>-->
        <field name="numbercall">-1</field>
    </record>
</odoo>
